<%- include("header") -%>

<div class="container">
    <h3 class="mt-2">Available Books</h3>
    <table class="table table-dark table-hover table-responsive mt-2">
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Category</th>
            <th>Description</th>
            <th>Delete</th>
        </tr>
        <tr>
            <td><%= bookItems.title %></td>
            <td><%= bookItems.author %></td>
            <td><%= bookItems.category %></td>
            <td><%= bookItems.description %></td>
            <!-- <td>
                <form action="/delete" method="post">
                    <button type="submit" class="btn btn-danger" name="delete" value="<%= bookItems._id %>">Delete</button>
                </form>    
            </td> -->
        </tr>
    </table>   
</div>

<%- include("footer") -%>